# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# --- Cache / broker ---
redis==5.0.8

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# Pinned: bcrypt>=4.1 removed the `__about__` attribute passlib 1.7.4's
# backend self-test relies on, causing a "password cannot be longer than
# 72 bytes" crash on the very first hash call. Keep pinned until passlib
# ships a fix (or migrate to bcrypt's native API directly).
bcrypt==4.0.1

# --- Document Intelligence ---
pymupdf==1.24.10
python-docx==1.1.2
openpyxl==3.1.5
langchain==0.3.1
langchain-community==0.3.1

# --- ML / Embeddings ---
sentence-transformers==3.1.1
transformers==4.44.2
torch==2.4.1
faiss-cpu==1.8.0

# --- LLM Providers ---
openai==1.51.0
anthropic==0.34.2
google-generativeai==0.8.1

# --- Policy engine ---
PyYAML==6.0.2

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.2
tenacity==8.5.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
